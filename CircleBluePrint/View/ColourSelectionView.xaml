<UserControl x:Class="SoloProjects.Dudhit.SpaceEngineers.SEBP.View.ColourSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="ColourBoxTemplate">
            <RadioButton HorizontalAlignment="Left" VerticalAlignment="Top" IsChecked="{Binding ColourIsChecked}"
                    TabIndex="{Binding ColourTabIndex}" GroupName="{Binding ControlGroupName}">
                <WrapPanel>
                    <Rectangle Fill="{Binding BoxColour}" Height="{Binding BoxHeight}" Width="{Binding BoxWidth}"></Rectangle>
                </WrapPanel>
            </RadioButton>
        </DataTemplate>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding ColourBoxes}" ItemTemplate="{StaticResource ColourBoxTemplate}">
  
    </ItemsControl>
</UserControl>
