<UserControl x:Class="SoloProjects.Dudhit.SpaceEngineers.SEBP.View.ColourSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <RadioButton x:Name="radBtn" HorizontalAlignment="Left" VerticalAlignment="Top"
            IsChecked="{Binding ColourIsChecked}" TabIndex="{Binding ColourTabIndex}" GroupName="{Binding GroupName}">
        <WrapPanel>
            <Rectangle x:Name="wreked" Fill="{Binding BoxColour}" Height="{Binding BoxHeight}"
                    Width="{Binding BoxWidth}"></Rectangle>
        </WrapPanel>
    </RadioButton>
</UserControl>
