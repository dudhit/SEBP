<UserControl x:Class="SoloProjects.Dudhit.SpaceEngineers.SEBP.View.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="Re_load User Settings" Command="{Binding ReloadSettings}"
                    ToolTip="Remembers location of blueprints, shape and Steam ID"  />
            <MenuItem Header="Sa_ve User Settings" Command="{Binding SaveUserSettings}"  />
            <MenuItem Header="E_xit" Command="{Binding MenuExit}" />
        </MenuItem>
        <MenuItem Header="O_ptions">
            <MenuItem Header="_Reset" Command="{Binding ResetToLoaded}" ToolTip="Restores defaults" />
        </MenuItem>
        <MenuItem Header="_Help" />

    </Menu>
</UserControl>
